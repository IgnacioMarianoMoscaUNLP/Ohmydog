<%- include ('vistas') %>
<body>    
    <h3 align="center">Turnos del Sistema</h3>
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Tipo</th>
                <th>Fecha</th>
                <th>Horario</th>
                <th>Descripcion</th>
            </tr>
        </thead>
        <tbody>
<<<<<<< HEAD
            <%if (data){%>
                    <% for (i=0;i<data.length;i++){%>
                      <tr>
                    <td><%=data[i].paciente%></td>      
                    <td><%=data[i].tipo%></td>
                    <td><%=data[i].dia%></td>        
                    <td><%=data[i].hora%></td>
                    <td><%=data[i].descripcion%></td>
                    <form action="/eliminarTurno" method="GET"> 
                      <input type="hidden" name="usuario" value="<%= data[i].paciente %>">
                      <input type="hidden" name="dia" value="<%= data[i].dia %>">
                      <input type="hidden" name="hora" value="<%= data[i].hora%>"> 
                      <td><button type="submit">Eliminar</button></td>
                  </form>
                  </tr>  
                <%}%> 
            <%}%>   
=======
            <% if (data) { %>
                <% for (i = 0; i < data.length; i++) { %>
                    <tr>
                        <td><%= data[i].paciente %></td>      
                        <td><%= data[i].tipo %></td>
                        <td><%= data[i].dia.getDate() %>/<%= data[i].dia.getMonth() + 1 %></td>        
                        <td><%= data[i].hora %></td>
                        <td><%= data[i].descripcion %></td>
                        <form action="/cancelarTurno" method="POST" onsubmit="return confirmarEliminacion(event)">
                            <input type="hidden" name="usuario" value=<%= data[i].paciente %>> 
                            <input type="hidden" name="hora" value=<%= data[i].hora %>>                             
                            <input type="hidden" name="dia" value="<%= data[i].dia %>"> 
                            <td><button type="submit">Eliminar Turno</button></td>
                        </form>
                    </tr>  
                <% } %> 
            <% } %>   
>>>>>>> 240f8ff8b7de43515162a14fc9b3228114c714ae
        </tbody>
    </table>
</body>
</html>
<%- include ('partials/_footer') %>

<script>
    function confirmarEliminacion(event) {
        event.preventDefault();
            // Mostrar confirmación al usuario
            var confirmacion = confirm("¿Seguro que desea eliminar el turno?");
            
            if (confirmacion) {
                // Enviar el formulario si el usuario confirma
                event.target.submit();
    }
}
    </script>


