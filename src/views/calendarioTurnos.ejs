<!-- archivo.ejs -->
<!DOCTYPE html>
<html>
<head>
  <title>Calendario de Turnos</title>
  <style>
    /* Estilos CSS para el calendario */
    .calendar {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: 5px;
    }

    .day {
      border: 1px solid #ccc;
      padding: 5px;
    }
  </style>
  <meta charset="UTF-8">
  <!-- Nombre -->
  <title> Oh my dog</title>
  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="Logo2.png">
  <!-- Styles -->
  <link rel="stylesheet" href="css/style.css">
  <!-- Boostrap-->
  <!--<link rel="stylesheet" href="https://bootswatch.com/5/united/bootstrap.min.css">-->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
</head>
<header>
  <a  href ="/" type="submit" class="btn btn-info-orange">Volver al Inicio</a>
    <center>
    <h1  aria-hidden="true" ></i>Calendario de Turnos</h1>
    </center>
</header>
<body>
  <center>
    <% let meses = ['enero', 'febrero', 'marzo', 'abril', 'mayo', 'junio', 'julio', 'agosto', 'septiembre', 'octubre', 'noviembre', 'diciembre']; %>
    <% let mes = mesActual%>
    <h2 style="text-transform: uppercase"><%= meses[mes]%></h2>
    
  </center>
  <div class="calendar">
    <% var j=0; %>
    <% for (let i = 1; i <= data1.length-1; i++) { %>
      <%  j++; %>
        <div class="day">
          <h2><%= i %></h2>
          <% if (data1[i] > 0) { %>
            <p>Turnos: <%= data1[i] %></p>
            <form action="/verTurnosDiax" method="POST">
              <input type="hidden" name="dia" value=<%= j %>> 
              <button type="submit">Ver Turnos</button>
            </form>            
          <% } else { %>
            <p>No hay turnos</p>
          <% } %>
        </div>
      <% } %>
  </div>
</body>
</html>
<%- include ('partials/_footer') %>
